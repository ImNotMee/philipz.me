import React, { Component } from "react";
import './nav.css';

class NavBar extends Component {


  render(prop) {
    return (
      <div id="navBar">
        <p id="myName">Philip Zhang</p>
        <button className="button-style" id="homeButton">ss</button>
      </div>
    )}
}

export default NavBar;
